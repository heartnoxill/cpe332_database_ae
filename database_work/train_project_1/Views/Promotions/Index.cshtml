@model IEnumerable<train_project_1.Models.Promotion>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Discount_percent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Start_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expiration_date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Discount_percent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Start_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Expiration_date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Promotion_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Promotion_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Promotion_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
