@model IEnumerable<train_project_1.Models.Price>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TotalPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Promotion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Route)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TotalPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class.C_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Promotion.Promotion_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Route.R_id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TicketT_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TicketT_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TicketT_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
